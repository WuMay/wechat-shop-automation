# 📜 所有脚本说明

## 🚀 运行脚本（主要使用）

### 1. edge-start.js ⭐⭐⭐
**用途**：Edge连接模式主启动脚本

**使用场景**：
- 想使用现有Edge登录状态
- 想实时看到操作过程
- 开发调试阶段

**运行方法**：
```bash
node edge-start.js "https://你的达人广场URL"
```

**特点**：
- 连接到调试模式的Edge浏览器
- 使用Edge的登录状态和Cookie
- 实时展示所有操作
- 不会关闭Edge浏览器

**前提条件**：
- Edge必须以调试模式启动（`--remote-debugging-port=9222`）
- 在Edge中已登录微信小店

---

### 2. index.js
**用途**：独立Chrome模式主启动脚本

**使用场景**：
- 想完全自动化
- 服务器环境运行
- 后台运行（无头模式）

**运行方法**：
```bash
node index.js "https://你的达人广场URL"
```

**特点**：
- 自动启动新的Chrome浏览器
- 独立的用户数据目录
- 首次需要手动登录
- 支持无头模式（`HEADLESS=true`）

**运行模式**：
```bash
# 显示浏览器
node index.js "URL"

# 无头模式
HEADLESS=true node index.js "URL"
```

---

## 🧪 测试脚本

### 3. test-edge-connection.js
**用途**：测试Edge浏览器连接

**运行方法**：
```bash
node test-edge-connection.js
```

**测试内容**：
- 连接到Edge浏览器
- 获取浏览器信息（版本、User-Agent）
- 检查打开的页面
- 测试创建新页面
- 测试页面导航
- 测试断开连接

**期望输出**：
```
========================================
Edge浏览器连接测试
========================================

步骤1: 测试连接到Edge...
✓ 连接成功

步骤2: 获取浏览器信息...
  - 版本: Microsoft Edge 120.x.x
  - User-Agent: Mozilla/5.0...

步骤3: 检查打开的页面...
  - 当前打开的标签页数量: 1

========================================
所有测试通过！
```

**何时使用**：
- 首次使用前
- 连接失败时
- 配置调试模式后

---

### 4. test.js
**用途**：环境测试（独立模式）

**运行方法**：
```bash
node test.js
```

**测试内容**：
- 检查Node.js版本
- 检查依赖是否安装
- 测试启动浏览器
- 测试页面加载
- 测试工具函数

**何时使用**：
- 安装依赖后
- 独立模式使用前
- 环境配置后

---

## 🎭 演示脚本

### 5. demo-edge.js ⭐
**用途**：Edge连接模式演示

**运行方法**：
```bash
node demo-edge.js
```

**演示内容**：
- 连接到Edge浏览器
- 创建新标签页
- 访问百度首页
- 模拟真人搜索
- 关闭标签页
- 断开连接

**特点**：
- 展示Edge连接的工作原理
- 让你看到实际操作过程
- 帮助理解自动化流程

**何时使用**：
- 首次使用Edge模式
- 想了解连接原理
- 测试调试模式是否正常

---

### 6. demo.js
**用途**：独立Chrome模式演示

**运行方法**：
```bash
node demo.js
```

**演示内容**：
- 启动Chrome浏览器
- 创建新页面
- 访问百度
- 模拟真人搜索
- 关闭浏览器

**何时使用**：
- 首次使用独立模式
- 想了解Chrome自动化

---

## 🔧 启动脚本

### 7. start-edge-debug.bat (Windows)
**用途**：启动Edge调试模式

**运行方法**：
```cmd
双击运行: start-edge-debug.bat
```

**功能**：
- 自动检测Edge安装路径
- 创建用户数据目录
- 启动调试模式的Edge浏览器
- 提示后续步骤

**特点**：
- 无需记住命令
- 自动处理路径
- 适合Windows用户

---

### 8. start-edge-debug.sh (macOS/Linux)
**用途**：启动Edge调试模式

**运行方法**：
```bash
./start-edge-debug.sh
```

**功能**：
- 检测Edge安装
- 创建用户数据目录
- 启动调试模式的Edge浏览器

**特点**：
- 支持macOS和Linux
- 自动检测Edge路径
- Shell脚本格式

---

### 9. start.sh
**用途**：快速启动脚本（独立模式）

**运行方法**：
```bash
./start.sh "达人广场URL"
```

**功能**：
- 检查依赖
- 安装依赖（如果需要）
- 运行 `node index.js`

**特点**：
- 一键启动
- 自动检查环境

---

## 📋 脚本使用流程图

```
首次使用流程：

1. 启动Edge调试模式
   Windows: 双击 start-edge-debug.bat
   macOS:   ./start-edge-debug.sh
   ↓
2. 在Edge中登录微信小店
   ↓
3. 测试连接
   node test-edge-connection.js
   ↓
4. 运行演示（可选）
   node demo-edge.js
   ↓
5. 运行自动化
   node edge-start.js "达人广场URL"


日常使用流程：

1. 启动Edge调试模式（如果未启动）
   ↓
2. 运行自动化
   node edge-start.js "达人广场URL"
```

---

## 📊 脚本对比表

| 脚本 | 模式 | 用途 | 使用频率 |
|------|------|------|---------|
| `edge-start.js` | Edge连接 | 主运行脚本 | ⭐⭐⭐ 经常 |
| `index.js` | 独立Chrome | 主运行脚本 | ⭐⭐ 经常 |
| `test-edge-connection.js` | Edge连接 | 测试连接 | ⭐ 偶尔 |
| `test.js` | 独立Chrome | 环境测试 | ⭐ 偶尔 |
| `demo-edge.js` | Edge连接 | 演示 | ⭐ 首次 |
| `demo.js` | 独立Chrome | 演示 | ⭐ 首次 |
| `start-edge-debug.bat` | Edge连接 | 启动Edge | ⭐⭐⭐ 每次 |
| `start-edge-debug.sh` | Edge连接 | 启动Edge | ⭐⭐⭐ 每次 |
| `start.sh` | 独立Chrome | 快速启动 | ⭐ 偶尔 |

---

## 🎯 推荐使用流程

### 新手用户（推荐）

```
第1天：
1. 双击 start-edge-debug.bat（启动Edge）
2. 在Edge中登录微信小店
3. 运行 node test-edge-connection.js（测试）
4. 运行 node demo-edge.js（看演示）
5. 修改 config.js 选择器
6. 运行 node edge-start.js "URL"（开始自动化）

第2天及以后：
1. 双击 start-edge-debug.bat（启动Edge）
2. 运行 node edge-start.js "URL"（自动化）
```

### 熟练用户

```
日常使用：
1. 双击 start-edge-debug.bat
2. node edge-start.js "URL"

调试时：
1. 双击 start-edge-debug.bat
2. node test-edge-connection.js
3. node edge-start.js "URL"
```

---

## 📝 命令速查

```bash
# 启动Edge调试模式
Windows: 双击 start-edge-debug.bat
macOS:   ./start-edge-debug.sh

# 测试连接
node test-edge-connection.js

# 运行演示
node demo-edge.js

# 运行自动化
node edge-start.js "https://你的达人广场URL"

# 独立模式
node index.js "https://你的达人广场URL"

# 无头模式
HEADLESS=true node index.js "URL"

# 查看日志
tail -f automation.log

# 查看已邀约记录
cat invited-talents.json
```

---

## ⚠️ 注意事项

1. **首次使用必须测试连接**
   - 运行 `test-edge-connection.js`
   - 确保Edge调试模式正常

2. **首次必须配置选择器**
   - 修改 `config.js`
   - 使用F12检查页面元素

3. **每次运行前启动Edge调试模式**
   - 使用 `start-edge-debug.bat`
   - 或手动运行命令

4. **保持Edge打开**
   - 脚本运行时不要关闭Edge
   - 不要关闭调试命令行窗口

5. **监控日志输出**
   - 观察控制台输出
   - 查看 `automation.log`

---

## 🎉 总结

**最常用的3个脚本：**

1. **start-edge-debug.bat** - 启动Edge（每次运行）
2. **edge-start.js** - 运行自动化（每次运行）
3. **test-edge-connection.js** - 测试连接（首次或出问题时）

**记住这个顺序：**
```
启动Edge → 测试连接（首次）→ 运行自动化 → 观察操作
```

就这么简单！🚀
