# Edge连接模式 - 快速开始（5分钟上手）

## 🚀 快速启动

### Windows用户（最简单）

1. **双击运行启动脚本**
   ```
   双击: start-edge-debug.bat
   ```
   这会启动调试模式的Edge浏览器

2. **在Edge中登录微信小店**

3. **在新的命令行窗口运行自动化脚本**
   ```cmd
   cd automation
   node edge-start.js "https://你的达人广场URL"
   ```

4. **看着Edge自动操作！** ✨

---

### macOS/Linux用户

1. **运行启动脚本**
   ```bash
   cd automation
   ./start-edge-debug.sh
   ```

2. **在Edge中登录微信小店**

3. **运行自动化脚本**
   ```bash
   node edge-start.js "https://你的达人广场URL"
   ```

4. **看着Edge自动操作！** ✨

---

## 📋 详细步骤（如果自动脚本不工作）

### 步骤1: 手动启动Edge调试模式

#### Windows

打开命令提示符（cmd），运行：
```cmd
"C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --remote-debugging-port=9222 --user-data-dir="%USERPROFILE%\EdgeUserData"
```

#### macOS

打开终端，运行：
```bash
/Applications/Microsoft\ Edge.app/Contents/MacOS/Microsoft\ Edge --remote-debugging-port=9222 --user-data-dir="$HOME/EdgeUserData"
```

#### Linux

打开终端，运行：
```bash
microsoft-edge --remote-debugging-port=9222 --user-data-dir="$HOME/EdgeUserData"
```

### 步骤2: 测试连接

```bash
node test-edge-connection.js
```

如果看到"所有测试通过！"，继续下一步。

### 步骤3: 配置选择器（重要！）

打开 `config.js`，找到 `business.selectors`，根据实际页面修改选择器：

```javascript
business: {
  selectors: {
    // 达人列表项
    talentItem: '.talent-item',

    // 详情按钮 - 需要修改！
    detailButton: '[class*="detail"]',

    // 邀请带货按钮 - 需要修改！
    inviteButton: '[class*="invite"]',

    // 添加商品按钮 - 需要修改！
    addProductButton: '[class*="add"]',

    // 确认按钮 - 需要修改！
    confirmButton: '[class*="confirm"]',

    // 发送邀约按钮 - 需要修改！
    sendInviteButton: '[class*="send"]',

    // 确认窗口 - 需要修改！
    confirmDialog: '[class*="dialog"]',

    // 下一页按钮 - 需要修改！
    nextPageButton: '[class*="next"]',
  }
}
```

**如何找到正确的选择器？**

1. 在Edge中打开微信小店达人广场
2. 按F12打开开发者工具
3. 点击元素选择器图标（Ctrl+Shift+C）
4. 点击页面上的按钮
5. 在Elements面板中查看class或id
6. 更新config.js中的选择器

### 步骤4: 运行自动化脚本

```bash
node edge-start.js "https://你的达人广场URL"
```

你会看到：
- Edge浏览器自动到达人广场页面
- 自动点击各个按钮
- 完成整个邀约流程

---

## 🔧 故障排除

### 问题1: 连接失败

**症状**: `ECONNREFUSED 127.0.0.1:9222`

**解决**:
1. 确认Edge以调试模式启动（命令包含 `--remote-debugging-port=9222`）
2. 检查端口是否被占用：
   ```bash
   # Windows
   netstat -ano | findstr :9222

   # macOS/Linux
   lsof -i :9222
   ```

### 问题2: 找不到按钮

**症状**: `未找到XXX按钮`

**解决**:
1. 修改 `config.js` 中的选择器
2. 使用F12检查页面元素
3. 确保页面加载完成

### 问题3: 页面加载超时

**症状**: `页面加载超时`

**解决**:
在 `config.js` 中增加超时时间：
```javascript
timeout: {
  pageLoad: 120000,  // 从60000增加到120000
}
```

---

## 📊 监控运行状态

### 实时查看日志
```bash
tail -f automation.log
```

### 查看已邀约达人
```bash
cat invited-talents.json
```

### 检查Edge连接
```bash
curl http://localhost:9222/json
```

---

## ⚠️ 重要提示

1. **首次运行前必须先配置选择器**
   - 不同版本的页面可能选择器不同
   - 使用F12检查元素是必须步骤

2. **首次登录后，登录状态会保存**
   - 保存在 `EdgeUserData` 目录
   - 下次无需再登录

3. **保持Edge打开**
   - 脚本运行时不要关闭Edge
   - 不要关闭调试命令行窗口

4. **不要频繁操作**
   - 避免触发反爬机制
   - 合理设置操作间隔

---

## 🎯 推荐工作流程

```
1. 启动调试模式Edge
   ↓
2. 在Edge中手动登录微信小店
   ↓
3. 使用F12检查页面元素
   ↓
4. 更新config.js中的选择器
   ↓
5. 测试连接: node test-edge-connection.js
   ↓
6. 运行脚本: node edge-start.js "URL"
   ↓
7. 观察Edge自动操作
```

---

## 📚 更多文档

- `EDGE-GUIDE.md` - Edge连接模式详细指南
- `README.md` - 完整项目文档
- `QUICKSTART.md` - 独立Chrome模式快速开始

---

## 🎉 开始使用

准备好了？现在开始：

```bash
# 1. 启动Edge调试模式
Windows: 双击 start-edge-debug.bat
macOS:   ./start-edge-debug.sh

# 2. 在Edge中登录微信小店

# 3. 测试连接
node test-edge-connection.js

# 4. 运行自动化
node edge-start.js "https://你的达人广场URL"
```

祝你使用愉快！ 🚀
